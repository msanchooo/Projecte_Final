import { VehicleListFilterPipe } from './vehicle-list-filter.pipe';

describe('VehicleListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
