import { ServeiListFilterPipe } from './servei-list-filter.pipe';

describe('ServeiListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ServeiListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
