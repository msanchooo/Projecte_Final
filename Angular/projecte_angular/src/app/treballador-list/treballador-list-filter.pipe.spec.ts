import { TreballadorListFilterPipe } from './treballador-list-filter.pipe';

describe('TreballadorListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TreballadorListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
