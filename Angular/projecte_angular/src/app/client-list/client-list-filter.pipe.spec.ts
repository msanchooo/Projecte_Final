import { ClientListFilterPipe } from './client-list-filter.pipe';

describe('ClientListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClientListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
